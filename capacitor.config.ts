import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agendadotecnico.com',
  appName: 'agenda-do-tecnico',
  webDir: 'dist'
};

export default config;
